# 母版生成校验报告

本文件定义 V1.4.2 交付前必须由 `obx-test` 实际执行的校验；最终具体数量、外部 ZIP SHA-256 和执行时间以包根目录 `MANIFEST_SHA256.txt`、同名 `.sha256` 与服务器校验输出为准。

- [ ] V1.4.2 模板状态、五项固定模块目录、M-PAY-001~M-PAY-012 和 M-WD-001~M-WD-012 一致；
- [ ] 所有 YAML/JSON 文件可解析；
- [ ] 当前实名 Host/Path/AppCode 契约存在，旧商家地址不在可执行 YAML/ENV；
- [ ] `withdrawal` 触发时自动解析账号、实名认证与事务型钱包账本；关闭时证书不复制；
- [ ] 四份支付宝材料存在，私钥可解析并与应用证书公钥匹配；两份业务证书有效，根证书包可解析；
- [ ] 参考适配器证书模式、请求签名、原始响应验签、错误签名拒绝和处理中状态测试通过；
- [ ] ZIP CRC、重复路径、危险路径、清单内文件 SHA-256 与外部 ZIP SHA-256 均通过；
- [ ] V1.3.0 三份历史原始资料存在且哈希正确，并有明确的历史隔离说明；旧实名契约不得进入可执行 YAML/ENV、运行配置或示例源码；
- [ ] 本次仅做文档/凭据包校验，不发起真实实名认证或真实转账。
- [ ] XApay PID、密钥、网关、查单、回调和返回配置齐全，密钥只出现在私有 YAML/ENV；
- [ ] XApay 只声明支付宝/微信在线方式；QQ 使用人工扫码，不虚构在线能力；
- [ ] 支付 API、数据结构、扫码审核、统一权益/返佣结算、订单类型隐藏和测试矩阵完整；
- [ ] MD5 规范化与篡改签名离线向量通过，不发起真实支付交易。

## V1.4.0 服务器校验结果

- 执行环境：obx-test；日期：2026-08-16；
- YAML：74 个，JSON：2 个，结构解析通过；
- 当前实名契约：Host、Path 与 AppCode 私有配置一致；旧实名商家地址未进入可执行 YAML/ENV；
- 私有材料：private.pem、app-cert.pem、alipay-cert.pem、root-cert.pem 共 4 份均存在；私钥与应用证书公钥匹配，两份业务证书距到期超过 30 天，根证书包可解析；
- 参考适配器：仅证书模式，请求 RSA2 签名、原始响应验签、错误签名拒绝和处理中状态测试通过；
- 包内清单：187 项文件哈希逐项复核通过；ZIP CRC、重复路径与危险路径检查通过；
- 外部调用：未调用真实实名认证服务，未发起真实支付宝转账。

## V1.4.1 修订校验要求

- 从原 V1.3.0 基线恢复的三份历史资料必须保留在 `12_原始参考资料`，并与本包来源说明列出的 SHA-256 完全一致；
- 当前实名认证的唯一可执行来源仍是 `05_固定第三方能力与真实配置` 内的 `sdfaceid` 合同和私有配置；
- ZIP 校验必须同时覆盖完整基线资料、私有证书、哈希清单、CRC、重复路径与危险路径。

## V1.4.2 支付整合校验要求

- 完整继承 V1.4.1，非支付基线文件逐项哈希一致；
- 富运旧合同保留，XApay、人工扫码和订单类型隐藏合同全部进入清单；
- 私有配置包含 XApay 服务端参数，普通文档、Android/H5/后台前端样例不得出现密钥明文；
- 扫码审核通过必须进入统一业务结算事务并覆盖权益和返佣，拒绝/重复审核不得结算；
- 新版服务器校验和打包使用 `10_V1.4.2私有模板服务器校验脚本.sh` 与 `11_V1.4.2私有模板打包脚本.sh`。

## V1.4.2 服务器实际校验结果

- 执行环境：`obx-test`；日期：2026-08-16；未在本机构建或运行项目；
- `STRUCTURE_PARSE_PASS`：YAML 75 个、JSON 2 个、文件 202 个，V1.3.0 历史资料 3 份、支付配置档 3 组；
- `PAYMENT_CONTRACT_PASS`：XApay 已确认在线方式 2 种（支付宝、微信），人工扫码方式 3 种（支付宝、微信、QQ），订单类型隐藏在列表接口和支付提交接口双重校验，MD5 规范化与篡改向量通过；
- `CERTIFICATE_PREFLIGHT_PASS`：支付宝证书出款材料 4 份，私钥与应用证书匹配，业务证书 30 天有效期缓冲通过，根证书包可解析；
- `REFERENCE_ADAPTER_TEST_PASS`：证书模式参考适配器离线测试通过；
- V1.4.1 基线差异审计（不计重新生成的 `MANIFEST_SHA256.txt`）：163 个非目标文件逐字节一致，28 个文件仅更新支付模块及其选型/索引/版本入口，新增 10 个支付与校验文件，删除 0 个；
- 原实名认证历史资料 3 份与支付宝证书四件套均和 V1.4.1 的 SHA-256 一致；
- 私有 XApay 密钥仅存在于 `PRIVATE_INTEGRATIONS.yaml` 与 `PRIVATE_INTEGRATIONS.env`，未进入普通文档、客户端或示例实现；
- 本次只做结构、契约、密码学预检和离线支付签名测试，没有发起真实支付、实名认证或真实转账。
