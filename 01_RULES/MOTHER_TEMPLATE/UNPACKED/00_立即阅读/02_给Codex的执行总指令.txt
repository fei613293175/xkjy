请立即读取本项目开发包并执行，不要重新规划与文档不同的方案。当前母版版本为 V1.4.2。

1. 读取 `RESOLVED_RULESET.yaml`，确认 G-001~G-047、轻量平台基线和启用的 M 模块。
2. 以 PROJECT_CONTEXT、CURRENT_RELEASE、FEATURE_MATRIX、PAGE_INDEX、RESOLVED_RULESET 和 RELEASE_MANIFEST 为唯一事实源。
3. 只开发当前 `/goal` 和已启用模块；未触发的平台能力禁止开发。
4. 连接 SSH `obx-test`；所有代码、数据库、后台、Android 构建、非设备测试和部署均在服务器完成。
5. 本机只使用已有 ADB 真机和接收交付物，禁止搭建项目、Docker、模拟器或虚拟设备环境。
6. 启用固定第三方能力时直接使用项目包中的私有配置；密钥只在后端服务器使用。实名认证只能使用当前 `sdfaceid` 套餐，禁止旧 `kzfacev1` 地址。支付必须按本包 payment profile 实现：富运旧通道、XApay支付宝/微信在线通道和支付宝/微信/QQ扫码人工审核通道均由服务端统一落单和结算。
7. 提现启用时直接使用项目包中的支付宝证书模式方案与四份 Secret 文件，不再读取旧插件；先完成非资金证书预检、假网关和状态机测试，真实转账必须有当前项目的明确授权。
8. 账号模块启用时完整实现注册、登录、找回密码、会话、账号后台和安全规则。
9. 图形安全验证码必须由后端自研，不接第三方：管理后台登录，以及 App 登录、注册、找回和发送验证码必须经一次性 captcha_ticket 后才处理业务；不得只在前端判断。
10. 标准 API、错误码、request_id、结构化日志、健康检查、最小配置、迁移备份、关键幂等和依赖锁定按轻量基线实现，不扩展为大型平台。
11. 所有页面实现返回状态保持：使用现有返回栈和页面状态容器，保留滚动、Tab、筛选、分页、草稿和已加载内容；禁止返回回顶、白屏、创建重复页面或在 onResume 无条件全量请求。
12. 所有图标按 `ICON_REGISTRY.yaml` 和随包 SVG 实现。通用功能使用统一 Remix Icon SVG，品牌使用官方资产，项目专属功能使用自定义 SVG；禁止文字、Emoji、符号或 Icon Font 代图标。
13. 管理后台按大型公司 App 平台的易用、可观测、可控和审计标准实现，但只覆盖当前真实业务。
14. 每次执行必须有实质编码、部署、修复或测试；每版最多新增一个平台型模块，核心业务优先。
15. 本版本完整测试新增、修改和受影响页面，阶段/最终版本执行全量回归。支付还要测试金额/签名/回调/查单幂等、支付方式按订单类型隐藏、扫码收款码随机选择、截图提交、后台审核后权益与返佣发放。
16. Android 交付前用状态为 `device` 的空闲真机安装、操作、检查崩溃/ANR/日志并截图对比；必须测试深度滚动进入子页面后返回的位置恢复和图标规范；不得用模拟器代替。
17. 测试后释放设备并返回主界面。
18. 未通过关键功能、启用模块联调、验证码绕过测试、页面状态恢复或视觉/图标验收时不得报告完成。
19. APK、中文功能对比、自动测试、真机报告、视觉报告和所有者测试说明统一放到桌面。
20. 完成 `/goal` 后更新状态、关闭目标并停止扩展。
