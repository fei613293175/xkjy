# 依赖锁定与可重复构建规范

- 固定 JDK、Gradle、AGP、Kotlin、Compose、后端运行时、数据库和容器基础镜像版本；
- 使用版本目录或锁文件记录第三方依赖；
- 每个第三方 SDK 记录用途、版本、权限和初始化位置；
- 构建记录 Git Commit、命令、环境、签名和产物 SHA-256；
- 依赖升级必须有明确原因、兼容验证和回滚方式；
- 不要求建设复杂供应链平台，但必须能够从同一提交重新构建可核验产物。
