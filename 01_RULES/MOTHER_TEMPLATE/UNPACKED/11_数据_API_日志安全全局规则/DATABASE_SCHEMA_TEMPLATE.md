# 数据库设计模板

每张表必须记录：

- 表名和业务目的；
- 主键和唯一键；
- 字段、类型、是否为空、默认值和说明；
- 外键或稳定关联；
- 状态枚举；
- 索引；
- 金额精度；
- 创建、更新和软删除；
- 敏感字段处理；
- 数据保留；
- 对应 API、App 页面和后台页面；
- 迁移脚本和回滚说明。

禁止为了页面展示直接建立无业务含义的字段；禁止用自由文本代替应受约束的状态和金额。
