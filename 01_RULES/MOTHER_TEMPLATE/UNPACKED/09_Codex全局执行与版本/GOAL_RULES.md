# /goal 规则

1. 每个小版本一个 `/goal`。
2. `/goal` 必须对应可运行、可测试、可交付的业务闭环。
3. 不允许同时开启多个相互竞争目标。
4. 当前目标完成前，不自行开始下一版本。
5. 阻断项必须有事实证据；能通过编码解决的不得长期标记阻断。
6. 完成标准包含功能、API、数据库、后台、UI、构建、测试和交付物。
7. 完成后更新状态文件并关闭 `/goal`。
8. 项目原则上控制在 15 个版本以内。
