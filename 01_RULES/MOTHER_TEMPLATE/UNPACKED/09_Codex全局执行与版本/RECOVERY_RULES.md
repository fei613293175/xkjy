# 中断恢复规则

恢复时按顺序检查：

1. Git 当前分支和最近提交；
2. `PROJECT_CONTEXT.yaml`；
3. `CURRENT_RELEASE.yaml`；
4. `DELIVERY_STATUS.yaml`；
5. 已部署服务和数据库迁移；
6. 最近构建、测试和问题报告；
7. 真机占用锁和设备状态。

恢复后先继续 `next_action`，禁止重新从头规划整个项目。

若状态文件与实际代码冲突，以代码、数据库和部署事实为准，并立即修正状态文件、记录冲突原因。
