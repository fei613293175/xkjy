# 参考实现

`node/alipay-certificate-payout.js` 是已在现有项目中完成签名、原始响应验签、证书材料预检、转账和查单测试的参考适配器。新项目可以按技术栈移植其语义，但仍必须：

- 接入项目自己的错误体系、日志脱敏、Secret 挂载和健康检查；
- 使用本模块状态机与数据库事务，不能只复制供应商调用文件；
- 保持证书模式，不回退到旧公钥字符串模式；
- 用假网关覆盖成功、处理中、失败、超时和错误签名；
- 在 `obx-test` 构建和测试，不在本机搭建项目环境。

参考实现不是可直接公开发布的独立 SDK，随私有母版使用。
