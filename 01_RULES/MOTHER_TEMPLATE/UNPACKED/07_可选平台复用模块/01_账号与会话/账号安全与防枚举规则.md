# 账号安全与防枚举规则

- 登录失败统一提示，不区分账号不存在、密码错误或账号未验证；
- 找回密码和发送验证码对外返回一致结果，避免确认账号是否注册；
- 邮箱、手机号和用户名在数据库中规范化并唯一；
- 密码、验证码、Token 和找回令牌均不可明文保存；
- 图形验证码票据必须用途绑定和一次性；
- 登录、注册、找回、发送验证码分别配置 IP、设备、账号和目标频控；
- 连续失败采用短时递增等待，禁止永久锁死目标账号；
- 管理后台登录同样使用图形验证码、频控、会话过期和审计；
- 所有安全失败记录 `request_id`，但日志不包含敏感值。
